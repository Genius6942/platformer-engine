{
  "name": "platformer-engine",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "transfer": "rm -r ./package/src/* && cp -R ./src/library/* ./package/src",
    "docs": "npx typedoc --out docs src"
  },
  "devDependencies": {
    "typescript": "^4.8.4",
    "vite": "^3.2.0"
  },
  "dependencies": {
    "typedoc": "^0.23.19"
  }
}
